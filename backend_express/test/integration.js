// Deprecated: this test was replaced by the focused `integration_kyc.js` test.
// Left intentionally inert to avoid accidental execution. If you need the
// original integration flow, recover it from git history.
console.log('integration.js deprecated -- no-op');
process.exit(0);
